import 'package:flutter/material.dart';

class AppColors {
  // Primary Color: Rich, deep gold (#D4A017) for buttons, headers, and key accents
  static const Color primaryGold = Color(0xFFD4A017);

  // Secondary Color: Sleek metallic silver (#B0BEC5) for backgrounds or secondary buttons
  static const Color metallicSilver = Color(0xFFB0BEC5);

  // Accent Color: Vibrant emerald green (#2E7D32) for active states and highlights
  static const Color emeraldGreen = Color(0xFF2E7D32);

  // Background Color: Dark charcoal (#212121) for a modern, high-tech vibe
  static const Color darkCharcoal = Color(0xFF212121);

  // Text/Icon Color: White (#FFFFFF) for contrast, with gold (#D4A017) for emphasis
  static const Color white = Color(0xFFFFFFFF);
  static const Color emphasisGold = Color(0xFFD4A017);

  // Button Colors
  // Default: Deep gold (#D4A017) with a subtle gradient to #F4E04D
  static const Color buttonDefaultStart = Color(0xFFD4A017);
  static const Color buttonDefaultEnd = Color(0xFFF4E04D);

  // Hover/Active: Emerald green (#2E7D32) with a slight glow effect
  static const Color buttonActive = Color(0xFF2E7D32);

  // Disabled: Light gray (#B0BEC5) with reduced opacity
  static const Color buttonDisabled = Color(0xFFB0BEC5);
}